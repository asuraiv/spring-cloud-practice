server:
  port: 20081

spring:
  application:
    name: msa-foo-app
  zipkin:
    baseUrl: http://127.0.0.1:9411/
  sleuth:
    enabled: true
    sampler:
      probability: 1.0

sample:
  zipkin:
    enabled: true

logging:
  level:
    org.springframework.boot.autoconfigure.logging: ERROR

